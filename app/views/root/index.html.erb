<% title "Homicide Watch Georgia" %>

<% if user_signed_in? %>
  <%= link_to('Create Report', new_admin_report_path, :class => 'btn btn-success btn-xlarge') %>
  <%= link_to('View Reports', admin_reports_path, :class => 'btn btn-primary btn-xlarge') %>
<% else %>
  <%= simple_format_no_tags(@about.content, {}, {sanitize: false}) %>
<% end %>
